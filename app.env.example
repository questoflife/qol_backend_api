# =============================================================================
# PRODUCTION ENVIRONMENT VARIABLES TEMPLATE
# =============================================================================
# 
# This is a template file showing the environment variables required for 
# production deployment of the QoL Backend API application.
#
# USAGE INSTRUCTIONS:
# 1. Copy this file to 'app.env' in the same directory
# 2. Replace the placeholder values with your actual production credentials
# 3. The 'app.env' file will be ignored by Git for security
#
# WARNING: Never commit actual production credentials to version control!
# =============================================================================

# --- DATABASE CONFIGURATION ---
# Replace these placeholder values with your actual production database credentials
DB_HOST=<YOUR_DATABASE_HOST>
DB_PORT=<YOUR_DATABASE_PORT>
DB_NAME=<YOUR_DATABASE_NAME>
DB_USER=<YOUR_DATABASE_USERNAME>
DB_PASSWORD=<YOUR_DATABASE_PASSWORD>

# --- APPLICATION SETTINGS ---
# Add any additional production environment variables below as needed

