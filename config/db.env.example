# Database configuration
# Used by both the database container and the application
# Copy this file to db.env and customize
# These settings override app.env

# NOTE: This file is REQUIRED when using any profile with a database ('db', 'dev-db', 'testing-db')
# The docker-compose.yml will fail to start without this file

# MySQL container settings
# These variables configure the MySQL container itself
MYSQL_ROOT_PASSWORD=rootpw
MYSQL_DATABASE=qol_db_with_test
MYSQL_USER=qol_user
MYSQL_PASSWORD=qol_pw

# Application database connection settings
# These variables tell your application how to connect to the database
# Values should match the MySQL settings above
DB_HOST=db
DB_PORT=3306
DB_NAME=qol_db_with_test  # Must match MYSQL_DATABASE above
DB_USER=qol_user          # Must match MYSQL_USER above
DB_PASSWORD=qol_pw        # Must match MYSQL_PASSWORD above
